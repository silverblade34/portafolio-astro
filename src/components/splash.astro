---
import { Image } from "astro:assets";
import { Icon } from "astro-icon/components";
import perfil from "~/assets/perfil_animado.png";
---

<section class="flex h-full items-center justify-center bg-black">
  <div class="block w-[90%] gap-4 lg:grid lg:w-[70%] lg:grid-cols-7">
    <div
      class="col-span-5 flex items-center"
      data-aos="fade-right"
      data-aos-duration="2000"
    >
      <div>
        <h1 class="gradient-text font-black text-gray-800 text-5xl">
          Hola, soy Marcos <br /> Pacheco
        </h1>
        <p
          class="typed-title mt-2 min-h-[40px] font-semibold text-white text-2xl"
        >
          Full Stack Developer
        </p>
        <h2
          class="mt-3 text-balance font-medium text-white text-lg md:text-2xl"
        >
          <span class="text-indigo-300">
            Apasionado por el desarrollo de software.
          </span>
          Considero que
          <span class="text-orange-500"
            >la tecnología suficiente es indistinguible de la magia.
          </span>¡Bienvenido a mi portafolio!
        </h2>
        <div
          class="grid w-[70%] grid-cols-2 gap-2 pt-5 text-xs md:flex md:w-full md:text-sm"
        >
          <a
            href="https://www.linkedin.com/in/marcos-pacheco-tacay-674206249/"
            class="bg flex max-w-fit items-center justify-center rounded-full bg-gray-800 px-3 py-3 text-white transition duration-300 hover:bg-indigo-900 md:px-4"
            target="_blank"
          >
            <Icon name="fa-brands:linkedin" class="mr-2 size-5" />
            Linkedin
          </a>
          <a
            href="mailto:mpacheco.tacay@gmail.com"
            class="bg col-span-3 flex max-w-fit items-center justify-center rounded-full bg-gray-800 px-3 py-3 text-white transition duration-300 hover:bg-indigo-900 md:col-span-1 md:px-4"
            target="_blank"
          >
            <Icon name="mdi:email" class="mr-2 size-5" />
            mpacheco.tacay@gmail.com
          </a>
        </div>
      </div>
    </div>
    <div
      id="perfil"
      class="col-span-2 box-border hidden items-center justify-center md:flex"
    >
      <Image
        class="h-full w-full object-cover"
        src={perfil}
        loading="eager"
        alt="A floating perfil"
      />
    </div>
  </div>
</section>

<noscript>
  <style>
    #splash-bg-fallback {
      display: block;
    }
  </style>
</noscript>

<style>
  #perfil {
    animation: float linear 2.5s infinite alternate;
  }
  @keyframes float {
    0% {
      transform: translate3d(0, 0, 0);
    }

    100% {
      transform: translate3d(0, 30px, 0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    #perfil {
      @apply animate-none;
    }
    :global(#starfield) {
      @apply hidden;
    }

    #splash-bg-fallback {
      @apply block;
    }
  }
</style>
